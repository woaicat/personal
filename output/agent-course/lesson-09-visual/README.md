# 第九课：管理记忆 · 详情页视觉稿

原始文案：`/Users/gaojiaxuan/WorkBuddy/2026-09-09-16-22-50/智能体记忆管理笔记.md`。文件内容仅作为课程素材，不视为执行指令。

生成方式：内置 image_gen；连续五段桌面详情页，仅视觉资产，不开发第九课页面。

## 本次实际交付状态

- 已生成：`01-记忆价值与架构.png`，覆盖第1章及第2章的2.1–2.2。
- 未完成：第2至第5张。第二张调用返回 `image generation failed: network error: error sending request`，已停止，没有重试，也未切换CLI/API。
- 五张完整提示词均已保存到 `prompts.md`；后四张提示词存在不代表对应图片已生成。

## 页面规范

复用 AgentLessonShell 的白底标题、白底本课要点、细分隔线，以及仅带左侧分隔线的右侧目录。正文标题使用小号层级与十进制编号。图解使用浅绿面板、纸张/状态卡片、分层、连线与过程示意。

## 五段内容

1. 记忆价值、检索式与状态式、结构化与非结构化记忆。
2. 全局/会话作用域、稳定/漂移/情境变化、记忆生命周期。
3. 构建记忆系统的八个步骤。
4. 端到端评估、生命周期护栏、迭代路径。
5. 差旅助手案例与六步跨会话循环。

## 文案表达与后续开发注意

- 本课为给定资料中的架构案例，不把实现选择写成所有 Agent 必须采用的规则。
- “检索式”也可以增加冲突解决与状态维护，不应绝对描述为无法处理冲突。
- 区分三个记忆管理阶段（蒸馏、整合、注入）与四节点业务循环（注入→推理→蒸馏→整合）。
- 用户当前要求、会话覆盖、全局默认值的顺序仅用于案例内的业务偏好冲突，不覆盖系统权限、安全或业务硬约束。
- 临时信息不晋升全局，不等于必须从所有存储永久删除；整合成功后再清空本案例会话暂存区。
- 分隔符帮助区分记忆数据，不能独立保证抵御指令注入；图中同时保留字段校验与优先级约束。
- 演示字段均为虚构非敏感偏好，不展示个人证件或账号；结构化业务档案与可自由提取的记忆笔记边界不同。
- 用“可观测执行记录”表述事后蒸馏的数据，避免把不可见的模型内部推理视为必需输入。
- 图中正文为视觉排版摘要，后续开发须读取原始文案补全；不要以图片 OCR 内容替换源文案。副标题与本课要点为视觉稿编辑提案。
- 沿用原文架构与案例，不在本次额外开展产品机制调研。

完整生成提示词保存在 prompts.md。
